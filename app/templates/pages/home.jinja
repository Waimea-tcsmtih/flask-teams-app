{#=====================================================
  Home page
  - Hero style greeting banner
  - List of things passed in as 'things' array
  - Form for adding a new thing
=====================================================#}

{% extends 'pages/base.jinja' %}


{% block title %}

    Flask Teams 

{% endblock %}


{% block content %}

    <h1>teams</h1>


    <article>

        <ul>
            {% for teams in teams %}

                <li class="teams">

   
                    
                    {{ teams.name }}
                
                    
                    {% if teams.completed %} 
                        <a href="/incomplete/{{ team.id }}">🗹</a>
                    {% else %} 
                        <a href ="/complete/{{ team.id }}">☐</a>
                    {% endif %}
                        <a href ="/delete/{{ team.id }}">Delete</a>
            
                   
                 
                </li>

                 
            {% else %}

                <p>None!</p>

            {% endfor %}
        
         
        
        
        
        </ul>

    </article>


    <article>

        <h3>Tasks</h3>

        <form action="/add" method="POST">

            <input
                type="text"
                name="name"
                placeholder="name of task"
                required
            >

            <input
                type="number"
                name="priority"
                min="0"
                max="5"
                placeholder="priority of task"
                required
            >

            <button>Add the Tasks</button>


        </form>

        
       

    </article>

{% endblock %}




{% block content %}

    <h1>Flask Demo App with Turso DB</h1>

    <p>This is a simple Flask app that connects to a Turso database to save and retrieve data. This can be used as the basis of your web app.</p>

    <p>It includes...</p>

    <ul>
        <li>Example routes</li>
        <li>Example static pages</li>
        <li>Missing page and server error pages</li>
        <li>DB connection and error handling</li>
        <li>Example CRUD actions: Create and Delete</li>
        <li>Flash messages for information</li>
    </ul>

    <a href="/things" role="button">Go to the Things...</a>

{% endblock %}

